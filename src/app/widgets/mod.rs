pub(super) use self::{
    fatty_acid::FattyAcidWidget,
    float::FloatWidget,
    indices::IndicesWidget,
    label::{Inner, LabelWidget},
    presets::Presets,
};

mod fatty_acid;
mod float;
mod indices;
mod label;
mod presets;
